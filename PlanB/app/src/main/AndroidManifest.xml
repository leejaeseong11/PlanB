<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.planb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".create_user"
            android:label="Create User" />
        <activity android:name=".destination_search"
            android:label="Destination Search"/>
        <activity android:name=".destination_regist"
            android:label="Destination Regist"/>
        <activity android:name=".guide_list"
            android:label="guide_list"/>
        <activity android:name=".selected_guide"
            android:label="Selected Guide"/>
        <activity android:name=".UpdateUser"
            android:label="Update User"/>
        <activity android:name=".destination_delete"
            android:label="My destination"/>
        <activity android:name=".UpdatePassword"
            android:label="Update password"/>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>